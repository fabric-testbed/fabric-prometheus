all:
  hosts:
  children:
    head_node:
      hosts:
        #Add head node host names here, fqdn, localname or ips.
        renc-metrics
    workers:
      hosts:
        #Add hosts names here, fqdn, localnames or ips.
        #renc-metrics

